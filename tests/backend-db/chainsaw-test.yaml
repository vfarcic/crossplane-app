apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: backend-db
spec:
  template: true
  steps:
    - try:
        - apply:
            file: install.yaml
        - assert:
            file: assert-install.yaml
      catch:
        - script:
            content: kubectl get app silly-demo --namespace $NAMESPACE --output yaml
        - script:
            content: kubectl get managed
