apiVersion: devopstoolkitseries.com/v1alpha1
kind: AppClaim
metadata:
  name: silly-demo-backend
spec:
  id: backend
  compositionSelector:
    matchLabels:
      type: backend
      location: local
  parameters:
    scaling:
      enabled: true
